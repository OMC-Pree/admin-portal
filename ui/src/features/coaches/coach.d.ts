export interface ICoachTableData {
  id: string;
  airTableId: string;
  firstName: string;
  lastName: string;
  email: string;
  // dateOfBirth: string;
  type: UserType;
  permissions: string;
  createdAt: string;
}
